query GetErrorPage($errorCode: String!) {
  getErrorPage(errorCode: $errorCode) {
    headline
    strapline
    errorImage {
      alt
      image {
        url
      }
    }
    link {
      title
      url
    }
  }
}
