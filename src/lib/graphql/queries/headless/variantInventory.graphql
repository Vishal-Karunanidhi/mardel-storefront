query GetVariantInventoryDetails($variantKeys: [String!]!) {
  getInventoryDetails(variantKeys: $variantKeys) {
    inventoryItems {
      productCode
      totalCount
    }
    productsNotFound
  }
}
